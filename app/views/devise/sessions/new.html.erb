<!-- This view is not used since we're using Auth0 for authentication -->
<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full">
    <div class="bg-white rounded-2xl shadow-2xl p-8 text-center">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">Authentication Disabled</h2>
      <p class="text-gray-600 mb-6">We use Auth0 for secure authentication.</p>
      <%= link_to "Login with Auth0", user_auth0_omniauth_authorize_path, 
                  class: "bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors" %>
    </div>
  </div>
</div>
